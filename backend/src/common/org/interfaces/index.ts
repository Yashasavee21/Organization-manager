export enum OrgUserRole {
  OWNER = 'owner',
  USER = 'user',
}

export enum OrgStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum OrgUserStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum InviteStatus {
  INVITED = 'invited',
  EXPIRED = 'expired',
  ACCEPTED = 'accepted',
}

export enum OrgBillingPlan {
  FREE = 'free',
  PAID = 'paid',
}
